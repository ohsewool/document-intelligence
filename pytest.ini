[pytest]
# `slow` is kept registered but currently unused: the full-document parse it was
# created for turned out to take seven seconds, not three minutes. The original
# figure was measured while another job held the CPU.
markers =
    slow: takes long enough that deselecting it is worth the loss of coverage
